SUPABASE_REST_URL=https://your-project-ref.supabase.co/rest/v1/
SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key

# Server-only secret. Required when the app has no Supabase login flow yet.
# Never expose this in Vite/NEXT_PUBLIC/client-side variables.


# On Render: omit PORT and SERVER_HOST entirely — Render injects PORT=10000
# automatically and config.js defaults SERVER_HOST to 0.0.0.0.
# Locally: use the values below.
PORT=1000
SERVER_HOST=0.0.0.0

# Vercel frontend — set this to your Render service URL so the frontend knows
# where to send API requests.
VITE_API_URL=http://localhost:1000
